# ─────────────────────────────────────────────────────────────────────────────
# Local Pseudonymization Tool — Dipendenze Python
# Target: Python 3.11 (testato e supportato)
# ─────────────────────────────────────────────────────────────────────────────

# Web framework
# uvicorn senza [standard] per evitare build di watchfiles/websockets/uvloop
# che richiedono compilatori C su alcune piattaforme Windows
fastapi>=0.104.0,<1.0.0
uvicorn>=0.24.0,<1.0.0
python-multipart>=0.0.6,<1.0.0

# Parsing documenti
python-docx>=1.1.0,<2.0.0
openpyxl>=3.1.2,<4.0.0
pypdf>=3.17.0,<5.0.0

# Immagini e OCR
Pillow>=10.1.0,<13.0.0
pytesseract>=0.3.10,<1.0.0
piexif>=1.1.3,<2.0.0

# Cifratura
# cffi NON elencato esplicitamente: cryptography lo gestisce come dipendenza
# transitiva con wheel precompilato su Windows (nessun compilatore C richiesto)
cryptography>=41.0.0,<47.0.0

# reportlab RIMOSSO: i report sono generati in HTML/JSON puri
# Non è mai usato nel codice applicativo (solo in tests/create_test_data.py
# con fallback automatico se non disponibile)
