# Development dependencies
pytest==8.0.0
pytest-cov==4.1.0
pytest-asyncio==0.23.5
pytest-mock==3.12.0

# Code quality
black==24.2.0
isort==5.13.2
flake8==7.0.0
pylint==3.0.3

# Security
bandit==1.7.7
# pip-audit sostituisce safety 3.x che richiede API key per funzionare in CI
# pip-audit non richiede autenticazione e usa il database OSV/PyPI ufficiale
pip-audit==2.7.3

# Type checking
mypy==1.8.0

# Testing utilities
faker==22.6.0
hypothesis==6.98.5

# Pre-commit
pre-commit==3.6.0
# Optional dependencies for enhanced features (have fallback handlers in code)
ldap3>=2.9.1,<3.0.0  # LDAP detection (optional, lazy-loaded with fallback)
reportlab>=4.0.0  # PDF generation in tests and transformer module (optional, lazy-loaded with fallback)
                 # reportlab 3.x non ha wheel precompilati per Python 3.12 (richiede ft2build.h a compile time);
                 # reportlab 4.x ha wheel precompilati per Python 3.12+ senza compilazione